plugins {
    id "com.github.node-gradle.node" version "7.0.1"
}

node {
    download = true
    version = '21.6.2'
}

tasks.register('npmBuild', NpmTask) {
    args = ['run', 'build']
    dependsOn npmInstall
}

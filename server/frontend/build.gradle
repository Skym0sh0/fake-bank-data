plugins {
    id 'org.siouan.frontend-jdk17' version '8.0.0'
}

frontend {
    nodeVersion = '21.6.2'
    nodeInstallDirectory = layout.buildDirectory.dir("node")


    assembleScript = "run build"
    cleanScript = "run clean"
    checkScript = "run check"

    verboseModeEnabled = true
}
